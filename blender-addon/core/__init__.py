"""Core data types — Bone, Sprite, Slot, Animation, Track.

Plain Python dataclasses, no `bpy` dependency. Lets the exporter and
the importers share a single in-memory representation that mirrors
the `.proscenio` schema.
"""
