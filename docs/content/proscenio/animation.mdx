---
title: Animation
sidebar_position: 5
---

import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import schema from "../../../packages/models/schemas/proscenio.schema.json";

export const withDefs = (name) => ({ ...schema.$defs[name], $defs: schema.$defs });

# Animation

A named clip is a set of `Track`s; each track is a list of `Key`s on a single target.

## Animation

<JSONSchemaViewer schema={withDefs("Animation")} />

## Track

<JSONSchemaViewer schema={withDefs("Track")} />

## Key

<JSONSchemaViewer schema={withDefs("Key")} />
