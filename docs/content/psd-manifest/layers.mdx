---
title: Layers
sidebar_position: 2
---

import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import schema from "../../../packages/models/schemas/psd_manifest.schema.json";

export const withDefs = (name) => ({ ...schema.$defs[name], $defs: schema.$defs });

# Layers

The layer kinds a manifest carries: a `polygon` layer (one PNG per layer) or a `sprite_frame` layer (a group of frames, each a `FrameEntry`).

## PolygonLayer

<JSONSchemaViewer schema={withDefs("PolygonLayer")} />

## SpriteFrameLayer

<JSONSchemaViewer schema={withDefs("SpriteFrameLayer")} />

## FrameEntry

<JSONSchemaViewer schema={withDefs("FrameEntry")} />
